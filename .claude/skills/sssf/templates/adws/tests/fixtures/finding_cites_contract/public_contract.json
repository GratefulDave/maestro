{
  "acceptance_criteria": [
    "verify-lane-geo-corpus-tests: Execution is from this role worktree/archive repository root (execution_context=\".\"). Every source is a required local path+SHA; read the full retained architecture fixture inventory from src-wp6-architecture-ir and the original pinned files, including all individually observed values, missing/null/empty/zero/partial distinctions and producer metadata. No source snapshot is a runtime import. All exact original verifier oracles below remain acceptance obligations; aggregation changes ownership and runner grouping only. The original baseline commands/floors remain mandatory supplemental cohorts and do not count as new expected-RED cases. At runtime record collection and executed per-file cohorts and reject a missing/deleted case even when the aggregate floor is met. Private tests are authored and sealed by the tests role; builder inputs contain the declared public API and behavior, never private test bytes. New test modules declare cases using only runner/stdlib imports and type-only TS imports at collection. Resolve actual new production imports inside cases/fixtures after collection, from the exact declared module paths. Missing new public production boundaries are intended case-level pre-build failures; missing dependencies, wrong import roots, config errors, collection failures, importorskip, fake fallbacks and mock-echo implementations are not acceptable RED. Never install scaffolds for absent production code. Parent validation must show every planned case collected before implementation and failed on its intended public behavior. Existing targets are inherited from current integration, never restored from immutable historical .txt copies. Finish all source-dependent changes under the declared sole file owners; preserve unrelated current integration edits and existing WP5 public/paid APIs. This authoring artifact authorizes no signing, run start, commit, projection publication, image build, or deployment by its author. Public boundary: Public @/lib/seo/llms buildLlmsTxt():string and buildLlmsFullTxt():string remain unchanged, no dynamic release inventory input. Exercise the actual outputs for all five entity/trend route families, conditional FAQ/label/regulatory descriptions, units/release/methodology/limitations and private route exclusion. Four new cases plus three unchanged llms.test.ts cases execute in the same direct command; paired floor4 counts the new cohort, not the three green baseline cases. Endpoint callers remain unchanged because signatures do not change. Sole output owners for this boundary: {\"lane-geo-corpus\": [\"src/lib/seo/llms.ts\"], \"lane-geo-corpus-tests\": [\"tests/wp6/geo-corpus.test.ts\"]}. Exact accepted architecture case cohorts: [{\"verifier_id\": \"verify-geo-corpus-refresh\", \"command\": \"npx vitest run --config tests/wp6/vitest.config.ts tests/wp6/geo-corpus.test.ts\", \"min_executed\": 4, \"oracle\": \"FUTURE CONTRACT: target test file is not implemented or executed at baseline 9d7045bc15b10f8fe094bcd4c71a899ad0f06f6c. These are author-chosen acceptance cases, not claimed existing test titles or runner counts: [\\\"llms.txt describes actual FAQ label and regulatory page capabilities with explicit release-evidence conditions, not fabricated per-release availability\\\", \\\"llms-full.txt describes units release methodology and limitations for new sections without claiming unobserved release values\\\", \\\"conditional section descriptions never promise that missing evidence exists; private API checkout remain excluded\\\", \\\"all public entity/trend route families remain represented after corpus refresh\\\"]. Downstream executable plan must create a tests/build pair using this exact direct whole-file argv and equal floor, tests lane owns this written target; record actual collection and intended pre-build red evidence before build. Count only candidate-created/modified runtime outputs, never planning artifacts or mocked producer echoes. Case4 explicitly checks all five canonical families: FAERS drugs/manufacturers and MAUDE devices/product-codes/manufacturers, each entity template plus /trend in both actual corpus outputs. This is new coverage, not attributed to the pinned llms.test.ts. Call the unchanged real buildLlmsTxt/buildLlmsFullTxt functions; verify actual emitted conditional descriptions and route URLs, not the Plan IR or fabricated release-inventory data. No dynamic input is introduced. Source alignment: execute the repository-relative command against candidate production modules, never specs/wp6-preparation evidence copies; pinned sources use access.artifact_path as their original module/test identity. Snapshot-path disjoint warnings remain a disclosed manual-review obligation, not a reason to substitute a green snapshot command.\", \"falsifiability\": {\"mutation\": \"remove one of the specified runtime behaviors or corrupt the produced value at its actual boundary\", \"expected_failure\": \"the corresponding named acceptance case fails on consumer-observable output\"}}, {\"verifier_id\": \"verify-llms-corpus\", \"command\": \"npx vitest run src/lib/seo/llms.test.ts\", \"min_executed\": 3, \"oracle\": \"Enumerated from the pinned tree at 9d7045bc15b10f8fe094bcd4c71a899ad0f06f6c: author-time declaration extraction from local snapshot specs/wp6-preparation/oracle-snapshots/src-oracle-verify-llms-corpus.txt (SHA-256 718fbb5f7f73e63069c927f8b1edf296d2260fac0ddcd75d112dd7135df54aca; upstream provenance is author-time evidence in specs/wp6-preparation/source-provenance.json) -> 3 titles: 'lists canonical entity and trend families'; 'keeps full context for chart HTML fallbacks'; 'allows crawlers, blocks API, and advertises the streamed sitemap'. The first case asserts methodology plus FAERS drugs and MAUDE devices /trend templates only; it does not iterate all five families or assert every entity parent. All-five parent/trend coverage is a future obligation of verify-geo-corpus-refresh; /checkout stays absent and /api/ stays disallowed. The oracle is the emitted corpus text. Current immutable enumeration source: local snapshot specs/wp6-preparation/oracle-snapshots/src-oracle-verify-llms-corpus.txt (SHA-256 718fbb5f7f73e63069c927f8b1edf296d2260fac0ddcd75d112dd7135df54aca; upstream provenance is author-time evidence in specs/wp6-preparation/source-provenance.json); no selector narrower than the complete existing file is introduced. Runtime not executed during preparation. Source alignment: execute the repository-relative command against candidate production modules, never specs/wp6-preparation evidence copies; pinned sources use access.artifact_path as their original module/test identity. Snapshot-path disjoint warnings remain a disclosed manual-review obligation, not a reason to substitute a green snapshot command.\", \"falsifiability\": {\"mutation\": \"remove the FAERS drugs /trend template from emitted llms.txt\", \"expected_failure\": \"'lists canonical entity and trend families' fails its literal FAERS drugs trend assertion; no claim that this baseline detects removal of the other three families\"}}] The pair shares one command and floor: npx vitest run --config tests/wp6/vitest.config.ts tests/wp6/geo-corpus.test.ts src/lib/seo/llms.test.ts; new-cohort floor=4. Test first, builder second, rerun the identical command. Preserve independently named per-file baseline floors in the accepted cohorts. Test-strength selectors below are author-chosen labels for future cases, not claims of existing test titles.  Future tests label positive and negative behavioral controls with wp6_corpus_positive / wp6_corpus_negative respectively; docs has one case containing both controls. These labels are private test-strength metadata, not new counted cases. The intended RED is the behavior named in this boundary, never missing generic tooling or a collection exception. Expected-RED diagnostic pattern is wp6_public_corpus_contract_mismatch. Behavioral assertions for existing modules use the specified diagnostic only when the actual consumer observation violates the contract, never an unconditional failure. Missing-module patterns name the intended new public production module, never generic tooling or collection errors. Every case in each named future cohort also carries its exact cohort label, in addition to applicable positive/negative labels. Minimums enforce distinct collected/executed cases, not assertion counts: [[\"wp6_corpus_case\", 4, \"req-wp6-corpus-public-contract\"]]. A case may not borrow a different cohort label. UI identity3 remains explicitly baseline-green; no fabricated RED credit.",
    "claim-wp6-corpus-boundary (positive): conditional public corpus descriptions must satisfy must never promise absent release evidence or advertise private API and checkout routes"
  ],
  "declared_outputs": [
    "tests/wp6/geo-corpus.test.ts"
  ]
}
