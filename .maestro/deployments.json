{
  "deployments": [
    {
      "name": "lexgenius-pipeline",
      "root": "../../lexgenius-pipeline/adws",
      "note": "Deployed instance. Level with the template; nothing declared."
    },
    {
      "name": "lexgenius",
      "root": "../../lexgenius/adws",
      "note": "Deployed instance. The five paths below are unresolved divergence tracked in issue #71, not settled deployment-owned surface: each holds content the template does not, and each needs both versions read. Remove the entry as each is decided. Three entries have been removed this way -- adw_modules/finalization_window.py, tests/test_finalization.py and tests/test_finalization_window.py held one fix and its test (the window arming its reviewer session before the first poll, without which §6.5's structural signals are unreachable), which is now upstream, so the template carries everything those copies had and mirroring them is safe.",
      "pinned": [
        "adw_modules/deliver.py",
        "tests/test_deliver.py",
        "tests/test_step10_cli.py",
        "tests/test_route_admission.py",
        "tests/test_step7_launcher.py"
      ]
    }
  ]
}
